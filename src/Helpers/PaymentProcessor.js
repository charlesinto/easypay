
class PaymentProcessor{
    static initPayment(){
        if(this.isCardInserted()){

        }else{
            
        }
    }
    /**
     * return true if card is insert or false if no card is found
     */
    static isCardInserted(){
        return false;
    }
}

export default PaymentProcessor;

